# Server configuration
PORT=4000
HOST=0.0.0.0

# JWT Secret — change this to a strong random string before first run
JWT_SECRET=CHANGE_ME_USE_A_LONG_RANDOM_STRING_HERE

# Admin credentials (used only for first-run seed; change after seeding)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin@PanicAlarm2024

# Database path (relative to server directory)
DB_PATH=./data/panic_alarm.db
